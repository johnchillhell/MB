name: blank

on:
  push:

jobs:
  gross:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        bureau: ["benefit", "consumption", "gasoline", "approximate", "expression"]
        minimum: ["worthy", "rarely", "perspective", "character"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar gI
